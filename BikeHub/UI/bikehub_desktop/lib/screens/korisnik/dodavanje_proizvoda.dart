import 'package:flutter/material.dart';

class DodavanjeProizvodaProzor extends StatelessWidget {
  const DodavanjeProizvodaProzor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Dodavanje proizvoda")),
      body: const Center(child: Text("Ovdje je ekran za dodavanje proizvoda")),
    );
  }
}